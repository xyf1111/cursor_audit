<?php
$h = static function ($value): string {
    return htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
};
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>请求详情</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 24px; color: #1f2937; }
        h1 { margin-bottom: 16px; }
        .card { border: 1px solid #e5e7eb; border-radius: 8px; padding: 20px; background: #fff; }
        .row { margin-bottom: 12px; }
        .label { display: inline-block; min-width: 120px; color: #6b7280; }
        pre { white-space: pre-wrap; word-break: break-word; background: #f9fafb; padding: 14px; border-radius: 6px; border: 1px solid #e5e7eb; }
        a { color: #2563eb; text-decoration: none; }
    </style>
</head>
<body>
    <h1>请求详情</h1>
    <p><a href="/admin/audit">返回列表</a></p>

    <?php if (empty($detail)): ?>
        <div class="card">未找到对应请求记录。</div>
    <?php else: ?>
        <div class="card">
            <div class="row"><span class="label">ID</span><?= $h($detail->id) ?></div>
            <div class="row"><span class="label">Trace ID</span><?= $h($detail->trace_id) ?></div>
            <div class="row"><span class="label">Session ID</span><?= $h($detail->session_id) ?></div>
            <div class="row"><span class="label">机器 ID</span><?= $h($detail->machine_id) ?></div>
            <div class="row"><span class="label">用户名</span><?= $h($detail->user_name ?: 'unknown') ?></div>
            <div class="row"><span class="label">事件时间</span><?= $h($detail->timestamp) ?></div>
            <div class="row"><span class="label">事件类型</span><?= $h($detail->event_type) ?></div>
            <div class="row"><span class="label">项目名</span><?= $h($detail->project_name) ?></div>
            <div class="row"><span class="label">模型名</span><?= $h($detail->model_name) ?></div>
            <div class="row"><span class="label">文件路径</span><?= $h($detail->file_path) ?></div>
            <div class="row"><span class="label">输入 Token</span><?= $h((int) $detail->input_tokens) ?></div>
            <div class="row"><span class="label">输出 Token</span><?= $h((int) $detail->output_tokens) ?></div>
            <div class="row"><span class="label">回答事件</span><?= $h($detail->response_event_type) ?></div>
            <div class="row"><span class="label">回答状态</span><?= $h($detail->response_status) ?></div>
            <div class="row"><span class="label">回答时间</span><?= $h($detail->response_at) ?></div>
            <div class="row"><span class="label">完成时间</span><?= $h($detail->request_finished_at) ?></div>
            <div class="row"><span class="label">创建时间</span><?= $h($detail->created_at) ?></div>
            <div class="row"><span class="label">更新时间</span><?= $h($detail->updated_at) ?></div>
            <div class="row">
                <span class="label">Prompt</span>
                <pre><?= $h($detail->prompt) ?></pre>
            </div>
            <div class="row">
                <span class="label">Response</span>
                <pre><?= $h($detail->response) ?></pre>
            </div>
        </div>
    <?php endif; ?>
</body>
</html>
